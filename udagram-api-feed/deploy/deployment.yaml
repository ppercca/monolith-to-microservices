apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-feed
  labels:
    app: app-feed
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-feed
  template:
    metadata:
      labels:
        app: app-feed
    spec:
      containers:
      - name: udagram-api-feed
        image: ppercca/udagram-api-feed
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 3
          periodSeconds: 3
